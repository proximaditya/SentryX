'use client';
import { useState } from 'react'; import { Reveal } from '../components/Reveal';
import { NewsCarousel } from '../components/NewsCarousel';
const articles=[['FIELD NOTE','Designing a resilient surveillance perimeter','A practical guide to making detection useful where bandwidth and visibility are limited.','sentryx-article-1'],['OPERATIONS','The anatomy of an actionable alert','Why context—not just notification speed—changes operator response.','sentryx-article-2'],['TECHNOLOGY','Edge AI without the black box','How open video standards enable a more accountable command center.','sentryx-article-3']];
export default function Resources(){const [done,setDone]=useState(false);return <div className="page"><section className="page-hero section"><p className="eyebrow">Intelligence library</p><h1>Ideas for a more <i>aware</i> operation.</h1><p className="lead">Notes from the field on video intelligence, resilient deployment, and better security decisions.</p></section><Reveal className="section editorial-grid">{articles.map(([type,title,copy,seed],i)=><article className="article-card" key={title}><img src={`https://picsum.photos/seed/${seed}/900/${i===0?700:550}`} alt=""/><div><p className="eyebrow">{type}</p><h2>{title}</h2><p>{copy}</p><button className="text-link">Read article <b>→</b></button></div></article>)}</Reveal><NewsCarousel /><section className="newsletter section"><div><p className="eyebrow">Signal, not noise</p><h2>A considered monthly dispatch.</h2><p>Practical intelligence for security and operations leaders. No sales clutter.</p></div>{done?<p className="form-success">You’re on the list. Welcome to the signal. <b>✓</b></p>:<form onSubmit={e=>{e.preventDefault();setDone(true)}}><input type="email" aria-label="Email address" required placeholder="Email address"/><button className="button dark">Subscribe <b>↗</b></button></form>}</section></div>}
